% =====================================================================
%  04 模型的建立与求解
%  论文的核心章节。每个问题一个小节,写明:
%   1. 建模思路(为什么用这个模型)
%   2. 符号定义(引用第 4 节的符号表)
%   3. 公式推导
%   4. 模型的解/算法(对应 code/src/ 里的代码)
% =====================================================================
\section{模型的建立与求解}

\subsection{问题一:传热与流动基本机理模型的建立与求解}

\subsubsection{控制方程}

基于第 3 节的模型假设,以计算流体力学与传热学理论为依据,对歧管式
微通道内冷却介质的流动与换热过程建立控制方程组:

\begin{itemize}
  \item 连续性方程(质量守恒):
  \begin{equation}
    \nabla \cdot \vec{v} = 0,
    \label{eq:continuity}
  \end{equation}

  \item 动量守恒方程(Navier--Stokes 方程):
  \begin{equation}
    \rho (\vec{v} \cdot \nabla)\vec{v} = -\nabla P + \mu \nabla^2 \vec{v},
    \label{eq:navier}
  \end{equation}

  \item 能量守恒方程(流体对流换热):
  \begin{equation}
    \rho C_p (\vec{v} \cdot \nabla)T_f = k_f \nabla^2 T_f,
    \label{eq:energy-fluid}
  \end{equation}

  \item 能量守恒方程(固体导热):
  \begin{equation}
    k_s \nabla^2 T_s = 0.
    \label{eq:energy-solid}
  \end{equation}
\end{itemize}

其中式~\eqref{eq:continuity}--\eqref{eq:energy-solid} 分别刻画了冷却液
的质量、动量与能量输运过程以及衬底内部的稳态导热过程,构成问题一机理
分析的基础。

\subsubsection{三项性能指标的数学定义}

在上述控制方程组所描述的流动换热过程基础上,定义系统的三项关键性能指标:

\begin{itemize}
  \item \textbf{热阻} $R_{th}$:反映系统将热量从芯片导出至冷却液的能力,
  \begin{equation}
    R_{th} = \frac{T_{max} - T_{in}}{Q},
    \label{eq:rth}
  \end{equation}
  其中 $T_{max}$ 为芯片表面最高温度,$T_{in}$ 为入口温度(293~K),
  $Q$ 为芯片总发热功率。

  \item \textbf{压降} $\Delta P$:反映冷却液流经系统全程的阻力特性,
  \begin{equation}
    \Delta P = P_{in} - P_{out}.
    \label{eq:dp}
  \end{equation}

  \item \textbf{温度非均匀性} $\Delta T$:反映芯片表面温度分布的均匀程度,
  \begin{equation}
    \Delta T = T_{max} - T_{min},
    \label{eq:dT}
  \end{equation}
  其中 $T_{min}$ 为芯片表面最低温度。此外,亦可用底面温度分布的标准差
  $\sigma_T$ 表征非均匀性,两种定义在本文后续分析中视情况选用。
\end{itemize}

\subsubsection{关键结构参数对性能指标的影响规律}

\textbf{(1) 针肋宽度比 $\alpha$ 的影响。}针肋宽度比增大时,微通道内
流体流通截面积减小,流速随之增大。在流动阻力方面,流速升高使摩擦阻力与
针肋前后形体阻力均增大,故压降 $\Delta P$ 增大;在换热方面,针肋体积
增大使有效换热面积增加,同时流体扰动与尾迹涡流增强,对流换热系数 $h$
提高,故热阻 $R_{th}$ 减小。

\textbf{(2) 歧管深高比 $\beta$ 的影响。}歧管深高比主要影响冷却液在
各条微通道之间的分配状态。适宜的深高比可降低流体分配的沿程阻力,使水流
均匀进入各微通道;若深高比过小,流量分配不均导致局部流量匮乏,芯片中心
与边缘温差扩大,温度非均匀性 $\Delta T$ 升高。该项指标对热阻与压降的
直接影响相对较弱,主要通过流量分配的均匀性间接作用。

\textbf{(3) 针肋排数 $n$ 的影响。}沿流动方向排数增加时,流体热边界层
被不断破坏与重建,流体混合更充分,换热性能显著增强,热阻 $R_{th}$ 明显
下降;同时,每一排针肋均产生局部阻力损失,排数增多使总阻力逐排叠加,
压降 $\Delta P$ 大幅升高。

\subsubsection{三项性能指标作为综合评价依据的合理性}

先进电子封装与热管理系统需要在有限空间、有限能耗以及复杂工况条件下实现
高效、稳定且均匀的散热。热阻、压降与温度非均匀性三项指标分别对应系统的
三大核心诉求:

\begin{itemize}
  \item \textbf{热阻——评估生存底线与绝对散热能力。}若热量不能及时
  导出,局部区域易形成"热点",导致结温升高、性能下降。热阻直观刻画了
  系统将热量散发出去的能力,是保障芯片不超温烧毁的最核心刚性指标。

  \item \textbf{压降——评估系统功耗与经济性。}微通道液冷通常伴随沿程
  压降较大的问题。压降直接决定驱动冷却液所需的外部泵功:压降越小,系统
  能耗越低,配套水泵的体积与成本也越低。该指标约束了不计成本追求散热的
  极端设计,保证方案的工程可行性。

  \item \textbf{温度非均匀性——评估可靠性与使用寿命。}芯片表面若存在
  较大温度梯度,极易引发材料热疲劳、界面失效等可靠性问题。温度分布越
  均匀,热应力越小,芯片物理寿命越长,系统长期运行越稳定。
\end{itemize}

综上,三项指标分别从散热效能、运行能耗与结构寿命三个维度对系统进行约束:
热阻保障散热能力下限,压降限制能耗上限,温度非均匀性维护结构可靠性,
三者相互独立、相互制衡,共同构成系统性能评价的完整依据,作为综合评价
指标具有科学性与合理性。

\subsubsection{模型求解与结果分析}

结果见表~\ref{tab:q1}:

\begin{table}[htbp]
\centering
\caption{问题一结果}
\label{tab:q1}
\begin{tabular}{lcc}
\toprule
项目 & 数值 & 说明 \\
\midrule
指标 A & 12.34 & 描述 \\
指标 B & 56.78 & 描述 \\
\bottomrule
\end{tabular}
\end{table}

结果如图~\ref{fig:q1} 所示:

\begin{figure}[htbp]
\centering
\includegraphics[width=0.8\textwidth]{figures/fig_q1.png}
\caption{问题一结果图}
\label{fig:q1}
\end{figure}

\subsection{问题二:高斯过程回归代理模型的建立与求解}

\subsubsection{样本数据特征分析与预处理}

基于附件 2 提供的 84 组实验/仿真样本,对自变量(结构参数)与因变量
(性能指标)定义如下。

输入向量(结构参数 $\mathbf{X}$):
\begin{equation}
  \mathbf{X} = [x_1, x_2, x_3]^T,
  \label{eq:input-vector}
\end{equation}
其中 $x_1$ 为针肋宽度比(取值范围 $0.00 \sim 0.30$),$x_2$ 为歧管深高比
(取值范围 $3.00 \sim 4.50$),$x_3$ 为针肋排数(取值范围 $0 \sim 10$)。

输出向量(性能指标 $\mathbf{Y}$):
\begin{equation}
  \mathbf{Y} = [y_1, y_2, y_3]^T,
  \label{eq:output-vector}
\end{equation}
其中 $y_1$ 为无量纲热阻 $R_{th}$(取值范围 $0.7219 \sim 0.7738$),
$y_2$ 为无量纲压降 $\Delta P$(取值范围 $0.0767 \sim 0.2040$),
$y_3$ 为无量纲温度非均匀性 $\Delta T$(取值范围 $0.7740 \sim 0.8731$)。

数据划分与标准化:按 $8:2$ 的比例将 84 组数据随机划分为 67 组训练集
(用于求解超参数并建立映射)与 17 组测试集(用于检验泛化预测能力)。
对输入特征矩阵进行 Z-score 标准化,消除量纲差异,提升算法收敛速度:
\begin{equation}
  \tilde{x}_i = \frac{x_i - \mu_i}{\sigma_i},
  \label{eq:standardize}
\end{equation}
其中 $\mu_i$、$\sigma_i$ 分别为第 $i$ 个特征在训练集上的均值与标准差。

\subsubsection{高斯过程回归代理模型的构建}

由于芯片的热流耦合物理场具有高度非线性,采用能够提供高精度非线性拟合与
不确定性表达的高斯过程回归建立多输入--单输出(MISO)映射模型。对于任意
一项性能指标 $y_j \in \{y_1, y_2, y_3\}$,假设其服从高斯过程先验分布:
\begin{equation}
  f_j(\mathbf{X}) \sim \mathcal{GP}\left(0, k(\mathbf{X}, \mathbf{X}')\right),
  \label{eq:gp-prior}
\end{equation}

选用的复合核函数由常数核(Constant Kernel)、径向基核(RBF Kernel)与
白噪声核(White Kernel)组成:
\begin{equation}
  k(\mathbf{X}, \mathbf{X}') = \sigma_f^2 \exp\left( -\frac{1}{2}
  \sum_{i=1}^{3} \frac{(x_i - x_i')^2}{l_i^2} \right) + \sigma_n^2
  \delta(\mathbf{X}, \mathbf{X}'),
  \label{eq:kernel}
\end{equation}
式中 $\sigma_f^2$ 为信号方差;$l_i$ 为对应结构参数的各向异性长度尺度
(Length-scale);$\sigma_n^2$ 为观测噪声方差;$\delta(\mathbf{X},
\mathbf{X}')$ 为 Kronecker delta 函数。

利用 67 组训练集样本,通过极大似然估计(MLE)对超参数集合
$\boldsymbol{\theta} = [\sigma_f, l_1, l_2, l_3, \sigma_n]$ 进行寻优
求解。对任意新的结构参数 $\mathbf{X}^*$,模型预测的均值为:
\begin{equation}
  \bar{y}^*_j = \mathbf{k}^T (\mathbf{K} + \sigma_n^2 \mathbf{I})^{-1}
  \mathbf{y}_{train},
  \label{eq:gpr-predict}
\end{equation}
其中 $\mathbf{K}$ 为训练集核矩阵,$\mathbf{k}$ 为训练集与新样本间的
协方差向量,$\mathbf{I}$ 为单位阵。

\subsubsection{模型拟合与预测效果评估}

采用决定系数 $R^2$、均方根误差 $RMSE$ 以及平均绝对百分比误差 $MAPE$
三项指标评估模型拟合与预测效果:
\begin{align}
  R^2 &= 1 - \frac{\sum_{k=1}^{N} (y_k - \hat{y}_k)^2}
       {\sum_{k=1}^{N} (y_k - \bar{y})^2},
  \label{eq:r2} \\
  RMSE &= \sqrt{\frac{1}{N} \sum_{k=1}^{N} (y_k - \hat{y}_k)^2},
  \label{eq:rmse} \\
  MAPE &= \frac{1}{N} \sum_{k=1}^{N} \left\vert
  \frac{y_k - \hat{y}_k}{y_k} \right\vert \times 100\%.
  \label{eq:mape}
\end{align}
其中 $\hat{y}_k$ 为第 $k$ 个样本的模型预测值,$\bar{y}$ 为观测均值,
$N$ 为样本数。$R^2$ 越接近 1 且 $RMSE$、$MAPE$ 越小,表明模型拟合
与预测效果越好。

\subsection{问题三:多目标优化模型的建立与求解}

问题三在问题二代理模型的基础上建立多目标优化模型。附件 2 样本数据的
Pearson 相关系数分析显示,热阻与压降之间存在明显的负相关($r = -0.684$),
即降低热阻的几何改动往往同时导致压降上升——三项性能指标之间固有的相互
制约关系,决定了不存在使所有指标同时达到绝对极小的单一点,其解集为
Pareto 最优解集(帕累托前沿)。因此本问采用"多目标寻优 + 综合决策"
两阶段策略:先利用带约束的多目标优化算法在参数空间内求取 Pareto 前沿,
再引入理想点法(TOPSIS 思想)将多目标问题转化为贴近理想解的单目标
综合决策问题,唯一确定综合最优设计。

\subsubsection{决策变量与约束条件}

定义决策变量矢量 $\mathbf{X} = [x_1, x_2, x_3]^T$,根据附件 2 样本数据
范围及实际物理含义,设置如下边界约束:
\begin{equation}
  \begin{cases}
    0.00 \le x_1 \le 0.30 & (\text{针肋宽度比边界}) \\
    3.00 \le x_2 \le 4.50 & (\text{歧管深高比边界}) \\
    0 \le x_3 \le 10, \quad x_3 \in \mathbb{Z}^+ & (\text{针肋排数整数约束})
  \end{cases}
  \label{eq:q3-constraints}
\end{equation}

\subsubsection{目标函数向量}

基于问题二求解得到的高斯过程回归代理模型 $f_{GPR, j}(\mathbf{X})$,
构造多目标向量最小化函数:
\begin{equation}
  \min_{\mathbf{X}} \quad \mathbf{F}(\mathbf{X}) =
  \left[ f_{Rth}(\mathbf{X}),\ f_{\Delta P}(\mathbf{X}),\
  f_{\Delta T}(\mathbf{X}) \right]^T,
  \label{eq:q3-objective}
\end{equation}
其中 $f_{Rth}$、$f_{\Delta P}$、$f_{\Delta T}$ 分别为热阻、压降与温度
非均匀性的代理模型预测函数。

\subsubsection{综合评价准则(理想点法,TOPSIS 思想)}

为消除热阻、压降与温度非均匀性之间的量纲及数量级差异,首先进行
Min--Max 线性无量纲归一化:
\begin{equation}
  \tilde{f}_j(\mathbf{X}) = \frac{f_j(\mathbf{X}) - f_{j,\min}}
  {f_{j,\max} - f_{j,\min}}, \quad j \in \{1, 2, 3\},
  \label{eq:minmax}
\end{equation}
式中 $f_{j,\min}$ 与 $f_{j,\max}$ 取自附件 2 数据集中各指标的极值边界。

理想最佳状态为 $\mathbf{O}^* = (0, 0, 0)^T$。按均等偏好(权值
$w_1 = w_2 = w_3 = 1/3$),建立基于欧氏距离最小化的综合优化目标函数
$D(\mathbf{X})$:
\begin{equation}
  \min_{\mathbf{X}} \quad D(\mathbf{X}) =
  \sqrt{ \sum_{j=1}^{3} w_j \cdot
  \left[ \tilde{f}_j(\mathbf{X}) \right]^2 },
  \label{eq:topsis}
\end{equation}
其中 $w_j$ 为第 $j$ 项指标的权重(问题四将进一步研究权重变化的影响)。

\subsubsection{模型求解算法流程}

求解算法采用多目标遗传算法(NSGA-II)与 L-BFGS-B 局部梯度搜索结合的
混合优化策略,具体流程如下:

\begin{enumerate}
  \item \textbf{全局空间采样}:使用带整数约束的多目标寻优算法在三维参数
  空间内快速搜寻,获得分布均匀的 Pareto 优选解集;
  \item \textbf{理想点贴近度计算}:对 Pareto 前沿上的各个解计算其综合
  目标值 $D(\mathbf{X})$;
  \item \textbf{离散整数修正}:考虑到针肋排数 $x_3$ 在实际工程中必须为
  整数排,对连续最优解中的 $x_3$ 进行离散整数约束网格搜索,二次微调
  $x_1$、$x_2$,锁定工程可行的最优组合。
\end{enumerate}

\subsection{问题四:权重敏感性分析与鲁棒设计}

\subsubsection{指标权重敏感性分析模型}

\textbf{(1) 场景偏好权重单纯形定义。}设无量纲化后的性能指标矢量为
$\mathbf{\tilde{F}}(\mathbf{X}) = [\tilde{f}_1(\mathbf{X}),\
\tilde{f}_2(\mathbf{X}),\ \tilde{f}_3(\mathbf{X})]^T$,分别代表无量纲
热阻、压降与温度非均匀性。引入描述应用场景偏好的权重矢量
$\mathbf{W} = [w_1, w_2, w_3]^T$,定义全场景偏好空间为二维标准单纯形
$\boldsymbol{\Delta}^2$(即 $\mathbb{R}^3$ 中的 2 维标准单纯形,几何上
为权重空间中的三角形区域):
\begin{equation}
  \boldsymbol{\Delta}^2 = \left\{ \mathbf{W} \in \mathbb{R}^3
  \;\middle\vert\; w_1 + w_2 + w_3 = 1, \quad w_j \ge 0, \;
  \forall j \in \{1,2,3\} \right\},
  \label{eq:weight-simplex}
\end{equation}

为直观刻画不同应用场景的偏好差异,选取三类典型场景,其权重组合
(分别对应热阻、压降、温度非均匀性)如表~\ref{tab:scenarios} 所示:

\begin{table}[htbp]
\centering
\caption{典型应用场景及其权重组合}
\label{tab:scenarios}
\begin{tabular}{llc}
\toprule
应用场景 & 特征描述 & 权重 $(w_1, w_2, w_3)$ \\
\midrule
高性能计算 & 重散热能力,允许较高泵功与温差 & $(0.6, 0.2, 0.2)$ \\
便携/低功耗设备 & 重泵功与能耗,散热压力相对较低 & $(0.2, 0.6, 0.2)$ \\
高可靠工业器件 & 重温度均匀性,关注长期可靠性 & $(0.2, 0.2, 0.6)$ \\
\bottomrule
\end{tabular}
\end{table}

\textbf{(2) 加权目标优化模型。}对于任意给定的场景偏好
$\mathbf{W} \in \boldsymbol{\Delta}^2$,建立加权理想点范数目标函数:
\begin{equation}
  \min_{\mathbf{X} \in \boldsymbol{\Omega}_{\mathbf{X}}} \quad
  D_{\mathbf{W}}(\mathbf{X}) = \sqrt{ \sum_{j=1}^{3} w_j \cdot
  \left[ \tilde{f}_j(\mathbf{X}) \right]^2 },
  \label{eq:weighted-objective}
\end{equation}
\begin{equation}
  \text{s.t.} \quad \begin{cases}
    0.00 \le x_1 \le 0.30 \
    3.00 \le x_2 \le 4.50 \
    0 \le x_3 \le 10, \quad x_3 \in \mathbb{Z}^+
  \end{cases}
  \label{eq:q4-constraints}
\end{equation}

\textbf{(3) 敏感性映射与偏好漂移方程。}偏好变化对最优解的影响可通过隐式
映射关系表达:
\begin{equation}
  \mathbf{X}^*(\mathbf{W}) = \arg \min_{\mathbf{X} \in
  \boldsymbol{\Omega}_{\mathbf{X}}} D_{\mathbf{W}}(\mathbf{X}),
  \label{eq:preference-map}
\end{equation}
通过求解偏好空间 $\boldsymbol{\Delta}^2$ 顶点极值点(极端场景)及内部
均匀网格点处的 $\mathbf{X}^*(\mathbf{W})$,计算决策变量关于权重的梯度
敏感性矩阵 $\mathbf{S}_{\mathbf{X},\mathbf{W}}$,用以定量评估参数敏感度:
\begin{equation}
  \mathbf{S}_{\mathbf{X},\mathbf{W}} = \begin{bmatrix}
    \frac{\partial x_1}{\partial w_1} & \frac{\partial x_1}{\partial w_2}
      & \frac{\partial x_1}{\partial w_3} \\
    \frac{\partial x_2}{\partial w_1} & \frac{\partial x_2}{\partial w_2}
      & \frac{\partial x_2}{\partial w_3} \\
    \frac{\partial x_3}{\partial w_1} & \frac{\partial x_3}{\partial w_2}
      & \frac{\partial x_3}{\partial w_3}
  \end{bmatrix}.
  \label{eq:sensitivity-matrix}
\end{equation}
需要说明的是,式~\eqref{eq:sensitivity-matrix} 中的偏导数建立在
$\mathbf{X}^*(\mathbf{W})$ 关于 $\mathbf{W}$ 分段光滑的假设之上。
由于最优解在 Pareto 前沿拐点处可能随权重变化发生跳变,本文在各采样点处
采用中心有限差分 $\frac{\partial x_i}{\partial w_j} \approx
\frac{x_i^*(\mathbf{W} + h\mathbf{e}_j) - x_i^*(\mathbf{W} -
h\mathbf{e}_j)}{2h}$ 进行数值近似($h$ 为步长,$\mathbf{e}_j$ 为第
$j$ 个坐标方向的单位向量),并在跳变点附近加密采样以刻画不连续特征。

\subsubsection{鲁棒设计优化模型(Minimax Regret 模型)}

\textbf{(1) 后悔值函数定义。}定义结构设计方案 $\mathbf{X}$ 在权重场景
$\mathbf{W}$ 下的后悔值 $R(\mathbf{X}, \mathbf{W})$:即该方案在此场景
下的实际评价值,与该场景下理论所能达到的最小(最优)评价值之差:
\begin{equation}
  R(\mathbf{X}, \mathbf{W}) = D_{\mathbf{W}}(\mathbf{X}) -
  D_{\mathbf{W}}\left( \mathbf{X}^*(\mathbf{W}) \right),
  \label{eq:regret}
\end{equation}
其物理含义为:$R(\mathbf{X}, \mathbf{W})$ 越大,说明方案 $\mathbf{X}$
在偏好 $\mathbf{W}$ 下相对于"定制最优方案"的性能劣化越严重。

\textbf{(2) 最小最大后悔值鲁棒优化。}鲁棒设计的核心思想是寻找一个能够
兜底最坏情况的方案,使得在全偏好空间 $\boldsymbol{\Delta}^2$ 内的最大
后悔值达到极小化:
\begin{equation}
  \min_{\mathbf{X} \in \boldsymbol{\Omega}_{\mathbf{X}}} \;
  \max_{\mathbf{W} \in \boldsymbol{\Delta}^2} \;
  R(\mathbf{X}, \mathbf{W}),
  \label{eq:minimax}
\end{equation}
展开写为双层极值优化问题(Bi-level Optimization Problem):
\begin{equation}
  \min_{\mathbf{X} \in \boldsymbol{\Omega}_{\mathbf{X}}} \left[
  \max_{\mathbf{W} \in \boldsymbol{\Delta}^2} \left(
  \sqrt{\sum_{j=1}^3 w_j \left[\tilde{f}_j(\mathbf{X})\right]^2} -
  \min_{\mathbf{X}' \in \boldsymbol{\Omega}_{\mathbf{X}}}
  \sqrt{\sum_{j=1}^3 w_j \left[\tilde{f}_j(\mathbf{X}')\right]^2}
  \right) \right],
  \label{eq:bi-level}
\end{equation}
式~\eqref{eq:bi-level} 的求解采用"外层枚举 + 内层采样"的数值策略:
\begin{enumerate}
  \item 对决策空间 $\boldsymbol{\Omega}_{\mathbf{X}}$ 中的候选方案
  $\mathbf{X}$(由问题三的 Pareto 前沿解集与网格候选点组成)逐一考察;
  \item 对每个候选方案,在权重单纯形 $\boldsymbol{\Delta}^2$ 上
  均匀采样 $N_W$ 个权重点(含顶点极端场景),对每个采样点求解内层最优值
  $D_{\mathbf{W}}(\mathbf{X}^*(\mathbf{W}))$(可调用问题二代理模型快速
  评估),取后悔值最大值作为该方案的最大后悔值;
  \item 比较所有候选方案的最大后悔值,取极小者即鲁棒解
  $\mathbf{X}_{robust}$。
\end{enumerate}
由于代理模型评估成本极低,该策略可在有限时间内完成全局搜索,且不受
决策空间维度增大的明显影响。

\textbf{(3) 方案稳定性评估指标。}引入综合性能变异系数(CV)与最大后悔
界限(Upper Regret Bound)评估方案的稳定性:
\begin{equation}
  CV(\mathbf{X}) = \frac{\sigma_{\mathbf{W}}\left(
  D_{\mathbf{W}}(\mathbf{X}) \right)}
  {\mu_{\mathbf{W}}\left( D_{\mathbf{W}}(\mathbf{X}) \right)},
  \label{eq:cv}
\end{equation}
式中 $\mu_{\mathbf{W}}$ 与 $\sigma_{\mathbf{W}}$ 分别为目标值在全偏好
空间上的期望与标准差。$CV(\mathbf{X})$ 越小,说明该方案对场景偏好波动
的免疫能力越强。

\subsubsection{鲁棒方案的判定依据与理论优势}

\textbf{(1) 判定依据。}
\begin{itemize}
  \item \textbf{风险下界保障(Minimax 准则)}:鲁棒解
  $\mathbf{X}_{robust}$ 满足最大后悔值 $R_{max} \le \epsilon$
  ($\epsilon$ 为容许上限),确保在极端偏好下不会出现性能崩溃;
  \item \textbf{变异平坦度(Robustness Flatness)}:在单纯形
  $\boldsymbol{\Delta}^2$ 内,目标响应面的梯度模长
  $\Vert \nabla_{\mathbf{W}} D_{\mathbf{W}}(\mathbf{X}) \Vert$
  趋近于最小值,说明函数值在权重维度上极度平缓。
\end{itemize}

\textbf{(2) 理论优势。}
\begin{itemize}
  \item \textbf{免除偏好主观依赖}:传统多目标优化极度依赖人工给定的
  权重,而本模型不假设任何先验偏好分布,能适应未知的应用场景变化;
  \item \textbf{锁定工程制造通用性}:芯片封装开模成本高昂,鲁棒方案
  提供了一套标准化、通用的结构尺寸参数,大幅降低了异构场景下的多规格
  开模与库存成本;
  \item \textbf{综合性能抗漂移}:有效卡住了热阻、压降与温度非均匀性的
  极值下限,兼顾了高效散热与低泵功运行的平衡。
\end{itemize}

\subsection{问题五:参数扰动敏感性与稳定性分析}

\subsubsection{参数小范围扰动模型}

\textbf{(1) 摄动变量概率分布表征。}在实际微纳加工制造与工程运行中,系统
不可避免地受到两类物理扰动:

\begin{itemize}
  \item \textbf{加工制造误差(静态扰动)}:由于光刻、蚀刻及封装工艺精度
  限制,导致针肋宽度比 $x_1$、歧管深高比 $x_2$ 及针肋排数 $x_3$ 存在
  尺寸偏差;
  \item \textbf{运行工况波动(动态扰动)}:系统在实际工作中受到入口质量
  流量 $\dot{m}$(基准 $1$~g/s)、入口温度 $T_{in}$(基准 $293$~K)以及
  芯片发热功率 $q_v$(基准 $5\times10^9$~W/m$^3$)的随机漂移。
\end{itemize}

定义系统综合摄动向量 $\mathbf{Z} = [\mathbf{X}, \mathbf{P}]^T \in
\mathbb{R}^6$,其中结构参数矢量 $\mathbf{X} = [x_1, x_2, x_3]^T$
(加工误差),工况参数矢量 $\mathbf{P} = [\dot{m}, T_{in}, q_v]^T$
(环境与负载波动)。假设各扰动变量在标称设计点
$\mathbf{Z}_0 = [\mathbf{X}_0, \mathbf{P}_0]^T$ 附近服从独立正态分布
$\mathcal{N}(\mu, \sigma^2)$ 或有界均匀分布 $\mathcal{U}[-\delta,
+\delta]$:
\begin{equation}
  \mathbf{Z} = \mathbf{Z}_0 + \boldsymbol{\delta}_{\mathbf{Z}}, \quad
  \boldsymbol{\delta}_{\mathbf{Z}} \sim \mathcal{N}\left(\mathbf{0},
  \boldsymbol{\Sigma}_{\mathbf{Z}}\right),
  \label{eq:perturb}
\end{equation}
其中协方差矩阵为对角阵
$\boldsymbol{\Sigma}_{\mathbf{Z}} = \text{diag}(\sigma_{x_1}^2,\
\sigma_{x_2}^2,\ \sigma_{x_3}^2,\ \sigma_{\dot{m}}^2,\
\sigma_{T_{in}}^2,\ \sigma_{q_v}^2)$。根据工程加工精度与控制要求,设定
相对扰动幅度 $\pm \varepsilon$。参考半导体封装工艺的典型公差水平
(光刻线宽误差 $\pm 1\%\sim3\%$、蚀刻深度误差 $\pm 3\%\sim5\%$)以及
运行控制精度,本文取 $\varepsilon = 5\%$ 作为综合扰动的保守上界进行
评估。

\textbf{(2) 扩展高斯过程响应曲面。}将问题二中的高斯过程代理模型扩展为
包含结构与工况的响应映射函数:
\begin{equation}
  y_j = G_j(\mathbf{Z}) = G_j(\mathbf{X}, \mathbf{P}), \quad
  j \in \{1, 2, 3\},
  \label{eq:gpr-extended}
\end{equation}
式中 $y_1, y_2, y_3$ 分别对应无量纲热阻、压降和温度非均匀性。需要说明
的是,附件 2 的样本数据在固定标称工况下采集,工况参数本身不参与训练;
本问通过以下方式实现工况维度扩展:将结构参数与工况参数统一按标称值做
相对偏差归一化($z_i \to z_i/z_{i,0}$),并以标称工况处的代理模型梯度
(由式~\eqref{eq:lsa} 的有限差分近似)线性外推工况扰动的影响,从而在
小扰动范围内($\pm 5\%$)保持足够的近似精度。该处理与"小范围扰动"的
问题设定一致,扰动幅度越大,线性近似的误差随之增大,本文评估以
$\varepsilon \le 5\%$ 为适用范围。

\subsubsection{性能指标敏感性分析模型}

为区分哪些参数的微小变化对散热性能影响最剧烈,分别建立局部敏感性模型
与全局敏感性模型。

\textbf{(1) 局部归一化敏感性系数(LSA/NSC)。}在标称设计点
$\mathbf{Z}_0$ 处对性能指标 $y_j$ 进行一阶泰勒展开,定义第 $i$ 个输入
参数对第 $j$ 个性能指标的无量纲局部敏感性系数 $S_{ij}^{local}$:
\begin{equation}
  S_{ij}^{local} = \left. \frac{\partial G_j(\mathbf{Z})}{\partial z_i}
  \right\vert_{\mathbf{Z}=\mathbf{Z}_0} \cdot \frac{z_{i,0}}{y_{j,0}},
  \label{eq:lsa}
\end{equation}
$S_{ij}^{local} > 0$ 表示正相关(参数增大导致指标上升);
$\vert S_{ij}^{local} \vert$ 模长越大,说明该指标对该参数的一阶局部
扰动越敏感。

\textbf{(2) 基于方差分解的 Sobol 全局敏感性分析(GSA)。}考虑高阶非线性
交叉耦合作用,将总方差 $V(y_j)$ 分解为单参数方差与多参数交互方差:
\begin{equation}
  V(y_j) = \sum_{i=1}^{k} V_i^j + \sum_{1 \le i < m \le k} V_{im}^j
  + \dots + V_{1,2,\dots,k}^j,
  \label{eq:sobol-decomp}
\end{equation}
定义一阶主效应指数 $S_i^j$(衡量单一参数 $z_i$ 的独立贡献率)与总效应
指数 $S_{Ti}^j$(衡量参数 $z_i$ 及其与其它所有参数联合交互的总贡献率):
\begin{equation}
  S_i^j = \frac{V_{z_i}\left( \mathbb{E}_{\mathbf{Z}_{-i}}[y_j \mid z_i]
  \right)}{V(y_j)},
  \label{eq:sobol-first}
\end{equation}
\begin{equation}
  S_{Ti}^j = \frac{\mathbb{E}_{\mathbf{Z}_{-i}}\left[ V_{z_i}(y_j \mid
  \mathbf{Z}_{-i}) \right]}{V(y_j)} = 1 -
  \frac{V_{\mathbf{Z}_{-i}}\left( \mathbb{E}_{z_i}[y_j \mid
  \mathbf{Z}_{-i}] \right)}{V(y_j)},
  \label{eq:sobol-total}
\end{equation}
式中 $\mathbf{Z}_{-i}$ 表示除 $z_i$ 外的其余变量集合。

\subsubsection{设计方案稳定性评估与不确定性传播模型}

\textbf{(1) 蒙特卡洛不确定性传播。}利用拉丁超立方采样(LHS)在扰动空间
$\mathcal{N}(\mathbf{Z}_0, \boldsymbol{\Sigma}_{\mathbf{Z}})$ 内抽取
$N$ 组扰动样本点 $\{\mathbf{Z}^{(1)}, \mathbf{Z}^{(2)}, \dots,
\mathbf{Z}^{(N)}\}$,代入代理模型进行不确定性传播映射,生成性能指标的
响应概率分布族 $\{y_j^{(1)}, y_j^{(2)}, \dots, y_j^{(N)}\}$。

\textbf{(2) 稳定性评价指标体系。}定义以下三个数学指标定量评价设计方案的
抗扰动稳定性:

\begin{itemize}
  \item \textbf{变异系数(Coefficient of Variation, CV)}:评估指标波动的
  相对分散程度,值越小说明稳定性越好,
  \begin{equation}
    CV_j^{pert} = \frac{\sigma_{y_j}}{\mu_{y_j}} =
    \frac{\sqrt{\frac{1}{N-1}\sum_{k=1}^N \left( y_j^{(k)} - \bar{y}_j
    \right)^2}}{\bar{y}_j},
    \label{eq:cv5}
  \end{equation}

  \item \textbf{95\% 置信区间波动上限(Upper Quantile Bound, P95)}:
  \begin{equation}
    y_{j, P95} = \text{Quantile}\left( \{y_j^{(k)}\}, 0.95 \right),
    \label{eq:p95}
  \end{equation}
  物理含义:在 95\% 的扰动工况下,性能指标不会恶化超过此安全阈值;

  \item \textbf{越界失效概率(Failure Probability, $P_{fail}$)}:设工程
  许可的最大性能恶化容限为 $y_{j, max}^{limit}$,则方案的失效概率定义为:
  \begin{equation}
    P_{fail, j} = \mathbb{P}\left( y_j > y_{j, max}^{limit} \right) =
    \frac{1}{N} \sum_{k=1}^{N} \mathbb{I}\left( y_j^{(k)} >
    y_{j, max}^{limit} \right),
    \label{eq:pfail}
  \end{equation}
  式中 $\mathbb{I}(\cdot)$ 为示性函数。
\end{itemize}

\subsubsection{敏感性与稳定性判定准则}

在完成求解后,根据以下三条理论准则对设计方案进行判定:

\begin{enumerate}
  \item \textbf{强敏感参数判定}:若某些参数的 Sobol 总效应指数
  $S_{Ti}^j > 0.2$,则判定该参数为系统关键控制参数,在工程加工中必须
  提高公差等级(如采用高精度微蚀刻加工);
  \item \textbf{高鲁棒性判定(稳定性合格)}:若在 $\pm 5\%$ 的综合随机
  扰动下,三项性能指标的变异系数均满足 $CV_j^{pert} < 3\%$,且越界失效概率
  $P_{fail, j} \approx 0$,则判定该优化方案具有强鲁棒性与高工程可靠性;
  \item \textbf{安全裕度判定}:比较置信上限 $y_{j, P95}$ 与系统极限
  熔断值的距离,距离越宽说明系统的工程容错能力与安全裕度越大。
\end{enumerate}

\subsubsection{设计方案的扰动验证闭环}

为验证前述各问设计方案的工程稳定性,将问题三的综合最优方案
$\mathbf{X}^*$ 与问题四的鲁棒方案 $\mathbf{X}_{robust}$ 一并代入本问
的扰动传播模型,在 $\pm 5\%$ 综合随机扰动下分别计算其变异系数
$CV_j^{pert}$ 与越界失效概率 $P_{fail, j}$,结果对比如
表~\ref{tab:q5-compare} 所示:

\begin{table}[htbp]
\centering
\caption{设计方案在 $\pm 5\%$ 扰动下的稳定性对比}
\label{tab:q5-compare}
\begin{tabular}{lcccc}
\toprule
方案 & $CV_1^{pert}$ & $CV_2^{pert}$ & $CV_3^{pert}$ & $P_{fail}$ \\
\midrule
综合最优方案 $\mathbf{X}^*$  & (代码手填入) & — & — & — \\
鲁棒方案 $\mathbf{X}_{robust}$ & (代码手填入) & — & — & — \\
\bottomrule
\end{tabular}
\end{table}

若鲁棒方案的变异系数与失效概率均不劣于综合最优方案,且满足
$CV_j^{pert} < 3\%$、$P_{fail, j} \approx 0$ 的判定准则,则表明问题四
所设计的鲁棒方案在加工误差与工况波动下依然保持稳定,进一步验证了从
"机理建模(问题一)"、"代理建模(问题二)"到"多目标优化(问题三)"、
"鲁棒设计(问题四)"、再到"扰动验证(问题五)"的完整建模链条的工程
可信度。
