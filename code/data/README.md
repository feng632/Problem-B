# code/data 说明

本目录存放比赛数据/中间数据。

**规则:**
- 原始数据(比赛附件)统一放这里,从官网下载后**只读**。
- 数据文件默认不提交 git(见 `.gitignore`);每个人在自己机器上把附件放进来即可。
- 建议在 `data/README.md`(仓库根目录)记录数据来源、字段含义,方便全队理解。
- 若数据有多个版本,按 `附件名_v2.xlsx` 命名,不要覆盖原文件。
