# 论文数据回填清单(编程手 ↔ 论文手交接)

> 本文档是"编程手跑数据 → 论文手回填"的交接单。
> **占位符约定**:论文中 `\textbf{(待回填:……)}` 与 `(代码手填入)` 是给编程手留的数值空位,
> 求解脚本跑完后由**论文手**逐个替换;替换时**不要删除占位符周边文字**,只换数字。
> 回填完成后按第 7 节清单自检,再 `make pdf`。
> **行号注意**:04-models.tex 与 06-sensitivity.tex 经段落重排后行号可能偏移,
> 下列"论文落点"行号仅供快速定位,精确位置以公式标签/表格 label 为准。

---

## 0. 总览

| 脚本(待编程手创建) | 输出内容 | 论文落点 |
|---|---|---|
| `code/src/solve_q1.py` | 机理关联式标定、影响幅度、Re(需附件1) | 5.1 节 + 6.1.2 节 |
| `code/src/solve_q2.py` | GPR 代理模型精度(R²/RMSE/MAPE) | 6.1.1 节 tab:model-check |
| `code/src/solve_q3.py` | 综合最优方案、回代验证偏差 | 5.3 节(需新增结果表)+ 6.1.3 节 |
| `code/src/solve_q4.py` | 三场景结果、敏感性矩阵、鲁棒方案 | 5.4 节(需新增结果表) |
| `code/src/solve_q5.py` | Sobol/LSA 指数、稳定性指标、扰动对比 | 5.5 节 + 6.2 节 |

**数据**:附件 2 已就位 `code/data/data.xlsx`(84 样本,列:样本编号/针肋宽度比/歧管深高比/针肋排数/无量纲热阻/无量纲压降/无量纲温度非均匀性,表头在第 2 行,读取时 `header=1`)。
**公共约定**:8:2 划分训练/测试集、**固定随机种子**(建议 42,写入脚本注释)、输入特征 Z-score 标准化;所有脚本输出时保留 4 位小数(百分比保留 2 位)。

---

## 1. solve_q1.py —— 问题一机理模型(5.1 节)

### 1.1 半经验关联式标定
论文落点:`paper/sections/04-models.tex:150`(eq:dp-corr)、`:157`(eq:rth-corr)

- 压降幂律: `ΔP* = C·β^a·(1-α)^b·n^c`,输出拟合系数 C、a、b、c 及 R²
- 热阻二次结构: `Rth* = R0 + c1·(α-α*)² + c2·β + c3·n`,输出 R0、c1、c2、c3、α*(最优宽度比)及 R²
- 回填位置:`04-models.tex` 5.1 节(2)的 `(待回填:由 code/src/solve_q1.py 输出)`

**论文侧预期(用于核对,方向已由数据验证)**:α* 应在 0.2 附近;压降关联式应显著解释数据方差。

**拍板记录(2026-08-12,建模手)**:两关联式 R² 均已接受(压降 R²=0.8769、热阻 R²=0.7486),不再要求改形式——关联式定位为机理方向验证与 α* 论证(热阻无量纲量程窄,R² 对拟合不敏感,MAPE 0.59%),定量预测职能归 GPR(6.1.1 节)。**α* 回填口径**:采用 GPR 交叉验证值 0.235(关联式二次项最优值 0.14 受 α·n 交互污染,仅作方向参考)。

### 1.2 影响幅度定量表(tab:q1)
论文落点:`paper/sections/04-models.tex:172`(tab:q1,第 3 列"幅度"列目前只有方向)

- 控制变量法:固定其余两个参数,统计各参数变化引起的指标平均相对变化(%):
  - α: 0.10→0.20(热阻下降幅度)、0.20→0.30(热阻回升幅度)、0.10→0.30(压降增幅)
  - β: 3.00→4.50(压降、温度非均匀性变化幅度)
  - n: 2→10(热阻降幅、压降增幅,并按 α 分档给出增幅对比)
- 回填:将幅度百分比写入表内或表下说明文字

### 1.3 影响规律图
论文落点:`paper/sections/04-models.tex:191`(fig:q1)

- 目前 `code/figures/fig_q1.png` 是示例图,**必须重画**(控制变量下 α-Rth 曲线族,标注拐点 α≈0.2 更佳)
- 脚本命名 `code/src/fig_q1.py`(或由 solve_q1.py 顺带生成),`make fig` 同步到 `paper/figures/`

### 1.4 机理规律与数据一致性检验的定量部分
论文落点:`paper/sections/06-sensitivity.tex:39-45`(6.1.2 节,"定量吻合程度(平均相对偏差等)待求解结果回填"处)

- 输出"平均相对偏差"等定量吻合指标(如:机理方向与数据一致的组合占比、关键规律的平均偏差 %)

### 1.5 Re 等流动参数计算 ✅ 阻塞解除(数量级估算已写入论文)
- ✅ 已按附件 1 物性完成并写入 5.1 节(2026-08-11):Pr = μ·C_p/k_f ≈ 7.0;
  Re ~ O(10)(以典型微通道水力直径作数量级估算),远低于转捩临界值 2300,
  层流假设(假设 3)成立
- ⚠️ 精确 Re 仍缺几何尺寸:附件 1 文字未给通道宽度/高度、针肋直径、歧管层
  高度等数值(在示意图 A1/A2 中);若图中有标注,编程手可补算精确值并核对
  数量级判断,无标注则保持现状(论文表述已自洽)

---

## 2. solve_q2.py —— 问题二 GPR 代理模型(6.1.1 节)

论文落点:`paper/sections/06-sensitivity.tex:24`(tab:model-check),占位符在 `:30-32`

- 对热阻、压降、温度非均匀性三个指标分别输出:**测试集** R²、RMSE、MAPE
- 输出约定与论文口径一致:留出法 8:2、测试集不参与训练、核函数 Constant+RBF+White
- 建议顺带输出:①训练集 R²/RMSE(论文手可加一句"训练-测试差距小,无过拟合");②对比基线模型(随机森林/多项式回归)的测试集 RMSE(加分项,一句话即可)

**论文侧预期**:R² 接近 1(仿真数据无噪声);若 R² < 0.98 需反馈。

---

## 3. solve_q3.py —— 问题三多目标优化(5.3 节)

### 3.1 综合最优方案 ⚠️ 论文目前没有结果表,需新增
- 输出:综合最优方案 **X\* = (x1\*, x2\*, x3\*)**(注意:针肋排数 x3 为整数,α>0 时 x3≥2,且 x1∈{0}∪[0.10,0.30] 的可行域耦合约束),及其对应的三项指标预测值(无量纲)与综合目标值 D
- 论文手在 5.3 节"模型求解算法流程"后**新增一张"综合最优设计方案"表**(参数 + 三指标 + D 值),并补 1-2 句结果讨论(如 α* 落在拐点附近、与样本中最优样本的对比)

### 3.2 回代验证偏差
论文落点:`paper/sections/06-sensitivity.tex:50`(6.1.3 节)

- 将 X\* 回代 GPR 与优化过程预测值比较,输出相对偏差百分比,替换 `(待回填:偏差百分比)`

### 3.3 Pareto 前沿(可选加分)
- 输出前沿图(三目标两两投影)或前沿解数量/分布信息,供论文手决定是否插图

---

## 4. solve_q4.py —— 问题四权重敏感性与鲁棒设计(5.4 节)

### 4.1 三场景最优方案 ⚠️ 论文目前只有权重表,缺结果,需新增
论文落点:`paper/sections/04-models.tex:381`(tab:scenarios,已有权重)

- 按表中三组权重 (0.6,0.2,0.2)/(0.2,0.6,0.2)/(0.2,0.2,0.6) 分别求解最优方案,输出各场景最优参数与目标值
- 论文手新增"三场景最优方案对比表"或直接在 tab:scenarios 后加结果列

### 4.2 权重敏感性矩阵
论文落点:`04-models.tex:360-440`(5.4.1 节,eq:sensitivity-matrix)

- 输出敏感性矩阵 **S_X,W** 数值(中心差分近似)或关键结论(哪个决策变量对权重最敏感、是否存在跳变)

### 4.3 鲁棒方案(Minimax Regret)
论文落点:`04-models.tex:442-499`(5.4.2 节)

- 输出:鲁棒方案 **X_robust**、其最大后悔值 R_max、容许上限 ε、全偏好空间上的 CV(问题四定义,eq:cv)
- 与等权综合方案 X\* 对比:两者参数差异、后悔值差异(论文手写"鲁棒方案以少量性能代价换取权重不敏感"之类结论的素材)

### 4.4 场景权重空间采样说明
- 输出权重单纯形上的采样方式与采样数 N_W(论文需写清),如均匀网格 + 顶点

---

## 5. solve_q5.py —— 问题五敏感性与稳定性(5.5 节)

### 5.1 结构参数层(严格,GPR 直接支撑)
论文落点:`04-models.tex:593-634`(5.5.2 节)

- **Sobol 指数**:对三个性能指标分别输出一阶主效应 S_i 与总效应 S_Ti(结构参数 x1,x2,x3)
- **LSA 局部敏感性系数**:eq:lsa,结构参数部分
- **蒙特卡洛**:LHS 采样 N(建议 ≥5000,写明 N),输出三项指标在 ±5% 扰动下的变异系数 CV、95% 分位数 y_P95、越界失效概率 P_fail(需先定义工程容限 y_lim,与论文手商定取值)

### 5.2 工况参数层(近似,线性化)
论文落点:`04-models.tex:584`(eq:op-linear,求和仅遍历工况参数)

- 对流量 ṁ、入口温度 T_in、发热功率 q_v 输出局部敏感性系数与线性化影响幅度,**单独报告**(不要与结构层混在一起)

### 5.3 验证闭环对比表(tab:q5-compare)
论文落点:`paper/sections/04-models.tex:702-709`(占位行在 :708-709)

- 将问题三 X\* 与问题四 X_robust 代入扰动传播,输出两者 CV_1/CV_2/CV_3 与 P_fail(结构层 MC + 工况层线性叠加)
- **判定准则核对**(04-models.tex:676-690):CV<3% 且 P_fail≈0 是否满足,满足与否都要如实写

### 5.4 6.2 节灵敏度分析结论 ⚠️ 必须与 5.5 一致
论文落点:`paper/sections/06-sensitivity.tex:60`

- 输出"最敏感参数 + 结论"一句话(如"热阻对 x1 的扰动最敏感"),**替换 6.2 节占位符时必须与 5.5 节 Sobol 结果一致**,两处不能打架

---

## 6. 论文手新增呈现物汇总(编程手跑完数据后要做)

| 新增内容 | 位置 |
|---|---|
| 5.3 节"综合最优设计方案"结果表 + 讨论 | `04-models.tex` 5.3 节末(已加 % 注释提醒) |
| 5.4 节"三场景最优方案"结果表(或给 tab:scenarios 加列) | `04-models.tex` 5.4.1 节(已加 % 注释提醒) |
| 5.4 节鲁棒方案结果表(参数/后悔值/CV,与 X\* 对比) | `04-models.tex` 5.4.2 节(已加 % 注释提醒) |
| 5.5 节 Sobol 指数表(3 指标 × 一阶/总效应) | `04-models.tex` 5.5.2 节(已加 % 注释提醒) |
| 5.2 节 GPR 最优超参数 θ\* 表(含长度尺度 l_i 解读) | `04-models.tex` 5.2.2 节(已加待回填标注) |
| **摘要五段**(每问一段结果数字:最优方案、R² 等) | `main.tex:33-40`(仍是"……"占位) |
| **参考文献**补真实文献(GPR/NSGA-II/Sobol/Minimax/微通道散热) | `08-references.tex`(两条示例占位) |
| **附录核心代码**(编程手贴入核心算法代码) | `09-appendix.tex` |
| 6.2 节灵敏度分析结论句 | `06-sensitivity.tex:60` |
| 图:fig_q1 重画;可选:Pareto 前沿图、Sobol 条形图、预测-实际散点图 | `code/src/*_fig.py` → `make fig` |

---

## 7. 回填完成后的自检清单(论文手)

1. `grep -rn "待回填\|代码手填入\|指标 A" paper/sections/` 应为空(注释里的除外);`main.tex` 摘要中的"……"占位已全部替换为结果句
2. 6.1.1 节正文"R² 均接近于 1、误差极低量级"的**定性表述与实际数字核对**,差距大则改措辞
3. 6.2 节最敏感参数结论 与 5.5 节 Sobol 指数排序一致
4. tab:q1 中"先降后升"方向与幅度数字不冲突;α* 与 5.1/6.1.2 的拐点表述一致
5. tab:q5-compare 中鲁棒方案不劣于综合最优方案的判定与 6.3 缺点第 2 条(Minimax 偏保守)的表述不矛盾
6. 新增的 5.3/5.4 表内可行域满足耦合约束(α=0 ⇔ n=0,α>0 ⇒ n≥2)
7. `make pdf` 通过;
